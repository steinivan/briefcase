import { CreateElementDirective } from './create-element.directive';

describe('CreateElementDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateElementDirective();
    expect(directive).toBeTruthy();
  });
});
